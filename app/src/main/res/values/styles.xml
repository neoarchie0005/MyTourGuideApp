<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Adjustments for ListItem.xml [ImageView] -->
    <style name="listItem_imageview">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <!-- Adjustments for ListItem.xml [Name TextView] -->
    <style name="listItem_nametextview">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text"></item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Adjustments for ListItem.xml [Description TextView] -->
    <style name="listItem_descriptiontextview">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text"></item>
        <item name="android:textColor">#424242</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">italic</item>
    </style>

    <!-- Adjustments for ListItem.xml [Address TextView] -->
    <style name="listItem_addresstextview">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:autoLink">map</item>
        <item name="android:drawableLeft">@drawable/ic_add_location_black_24dp</item>
        <item name="android:text"></item>
        <item name="android:textColor">#424242</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Adjustments for ListItem.xml [Phone TextView] -->
    <style name="listItem_phonetextview">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:autoLink">phone</item>
        <item name="android:drawableLeft">@drawable/ic_local_phone_black_24dp</item>
        <item name="android:text"></item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>
